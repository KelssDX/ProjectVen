# Contracts Placeholder

This directory hosts shared `Zod` API contracts and DTO schemas.

Current baseline includes:

- SQL-enum parity schemas in `enums.ts`
- API response envelope schemas in `envelope.ts`
- Auth request/response schemas in `auth.ts`

Use `index.ts` as the shared export surface.
